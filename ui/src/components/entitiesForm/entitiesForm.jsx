export const EntitiesForm = (props) => {
  const { data, goToPreviousPage, onUpdate, isGlobal, ...extensionProps } = props;
  const {
    lib: { React },
  } = extensionProps;

  return <div>Entities will be here</div>;
};
